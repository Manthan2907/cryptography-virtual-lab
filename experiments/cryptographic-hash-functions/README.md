# Cryptographic Hash Functions

Study and implement a cryptographic hash function to generate a fixed-length hash value for a given input message.

## Input
- Text message / binary input string

## Output
- Fixed-length cryptographic digest in hexadecimal representation

## Test
- Test with standard test vectors (e.g. empty string, single character change) to verify deterministic length and one-way hashing behavior.
